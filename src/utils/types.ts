export enum CookingStatus {
  Idle = 'idle',
  Raw = 'raw',
  Cooking = 'cooking',
  Done = 'done',
  Overcooked = 'overcooked',
}

export enum GameStatus {
  Ready = 'ready',
  Start = 'start',
  End = 'end',
}
